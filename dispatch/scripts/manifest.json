{
  "_comment": "QI Executor whitelist — THE security boundary for remote script execution. Scripts must be plain filenames living in this folder (C:\\QIH\\dispatch\\scripts). sha256='ANY' is acceptable while a script is still changing; pin the real hash once it is stable (PowerShell: (Get-FileHash .\\Setup-SynVox.ps1).Hash) so a modified script refuses to run until re-approved. NEVER put secrets in a script or in this file — API keys live in machine environment variables (setx).",
  "_updated": "2026-08-16",
  "scripts": {
    "setup-synvox": {
      "file": "Setup-SynVox.ps1",
      "description": "SynVox Phase 0 - MatrAIx engine bootstrap: prerequisites (git/uv/Python 3.12/Node 20+), Docker check-only, clone MatrAIx into C:\\APPS\\SynVox\\engine\\matraix, uv venv + editable installs, Persona 1M dataset download, free Docker smoke test. Idempotent and master-logged to C:\\APPS\\SynVox\\logs. Never runs a paid persona simulation.",
      "sha256": "ANY",
      "allowed_args": [],
      "machine": "POWERSPEC"
    },
    "synvox-smoke": {
      "file": "Test-SynVoxSmoke.ps1",
      "description": "SynVox Phase 0 acceptance gate - FREE. Runs 'matraix smoke' (upstream host-lane onboarding smoke) over every Survey task in the engine: loads the questionnaire, renders a persona, runs the real survey runner against a deterministic fake client, checks the answer envelope. No Docker, no provider calls, no ANTHROPIC_API_KEY, cost $0 - it CANNOT spend money. Answers are synthetic and not persona-faithful: this proves the harness works, it is never a simulation result. Logs to C:\\APPS\\SynVox\\logs\\synvox_smoke_<timestamp>.log.",
      "sha256": "ANY",
      "allowed_args": [],
      "machine": "POWERSPEC"
    },
    "qi-health-snapshot": {
      "file": "Get-QIHealth.ps1",
      "description": "Read-only QI machine health snapshot: every QI_* Windows service state, disk space per drive, and a /health probe of every registered project port. Logs to C:\\QIH\\logs\\qi_health_<timestamp>.log.",
      "sha256": "ANY",
      "allowed_args": [],
      "machine": "POWERSPEC"
    },
    "install-synvox": {
      "file": "Install-SynVox.ps1",
      "description": "SynVox install configuration - complements setup-synvox (which builds the engine). Verifies the engine/venv/Persona 1M are already healthy (refuses to run and exits non-zero if not, pointing at setup-synvox rather than re-bootstrapping), writes the SYNVOX_EDITION and bind address (config\\synvox.json + a user env var), verifies the Claude Max subscription CLI is headless-authenticated via qi_claude_brain.cli_status() and writes bin/env-file REFERENCES ONLY into config\\ai_providers.json, generates the SynVox API bearer token into secrets\\ (0600-equivalent, never logged), and optionally (-InstallServices) registers QI_SynVoxAPI as an NSSM service. QI_SynVoxRouter is registered only with the additional -Force flag, with a loud warning, because it drives a PERSONAL Claude Max subscription and NSSM services run under a service account. Master-logged to C:\\APPS\\SynVox\\logs\\synvox_install_<timestamp>.log.",
      "sha256": "ANY",
      "allowed_args": [],
      "machine": "POWERSPEC",
      "_note": "This script is normally run INTERACTIVELY, as the person whose Claude Max plan is being configured, with -ClaudeBin/-ClaudeEnvFile pointing at their own claude.exe and OAuth env file. allowed_args is deliberately [] so a remote qi_execute_script('install-synvox') call can only run it with all defaults, never with someone else's provider paths: the QI Connector executor runs as NT AUTHORITY\\SYSTEM, which is the wrong principal to drive a personal subscription lane (same reasoning as the QI_SynVoxRouter -Force gate above)."
    }
  }
}
