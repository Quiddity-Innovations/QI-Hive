# MilkWise (milkwise) — L2 brief

_Generated 2026-09-20 02:33:55_

## Registry facts
- Path: `C:\APPS\MilkWise`
- Status: active_development
- Phase: PWA proof of concept v0.1.0 — public at https://milkwise.vercel.app
- Ports: ui:7848, api:None
- Notes: Hosting is a MANUAL Vercel CLI deployment (Deploy_MilkWise.bat) — a git push does not redeploy. Governing docs live in the repo under 'Quiddity Innovations - MilkWise Documentation\Technical Documentation' (design doc, assessment D1-D21, findings F1-F21). Do not add analytics, third-party scripts, image persistence or any diagnostic claim.

## Brain
- No current_state recorded.
- No project-scoped decisions recorded.

## CLAUDE.md rules
# MilkWise — project notes for Claude
  - Printables live in the app at `#/print`; `print.html?sheet=gauge|tape|calibration|all[&auto=1]` renders the mm-true SVGs at `@page` margin 0. All SVGs are generated by scratch scripts (`make_gauge.py`) — regenerate, don't hand-edit. Keep essential content above y = 279 mm so Letter paper works.
  - `js/config.js` defaults (everything overridable in the Configuration screen), `store.js` schema v1 persistence, `rules.js` Sizing Rule Engine + confidence gate, `capture.js` camera/QA/coaching/mark tool, `screens.js`, `app.js` router.
- `.claude/launch.json` — `preview_start` config `milkwise-pwa` for browser testing.
- `tools/make_brand_assets.py` — the ONLY way `app/assets/logo*.png`, `wordmark.png` and every
  (drop mark over a "milkwise" wordmark). Regenerate, never hand-edit; re-run it if the master changes.

**Brand (re-pinned to the master logo 2026-09-17)**
- Sizing offset is a versioned rule set, never a constant (D3). Two ship: `traditional_v1`, `close_fit_v1`. `rule_set_id` logged on every record.
- Confidence is a predicted 95 % interval in mm; tiers relative to ladder step (D7). Four result states: HIGH / MEDIUM / LOW / UNMEASURABLE (D19).
- Catalogue ladders are config, target diameter shown before brand snap (D20). Feature name is not "Flange Fit" (D4).
- Schema v1 carries install_id, measurement_id, outcome FK, app/algorithm/schema/rule-set versions, both axes, QA scores, consent version (D8).
- Images are never persisted; geometry in mm may be (D14). Exports carry dates not times, demo records excluded (D15).
- Demo records (`capture_method = demo`, `is_demo = true`) are never pooled with real ones.
- **Printed sizing ring** (`capture_method = ring_gauge`, owner idea 2026-09-11): concentric pop-out rings; the aperture that fits is the measurement; the ring sits on the base plane so it doubles as a coplanar reference for the optional photo cross-check. Printable two-sided gauge: `app/gauge.html` (+ `assets/ring_gauge_front|back.svg`, generated by a script — regenerate rather than hand-edit). Config block `ring_gauge`. Material (die-cut card / kiss-cut vinyl / fabric) is an open study item.
- **Soft measuring string** (`capture_method = string_wrap`, owner idea 2026-09-11): wrapped around the nipple **base — never the areola** (design §2.2); equivalent diameter = L/π with a tension correction; optional photo laid straight (graduations = scale, `roundOnly`) or kept in shape (both axes, needs shape-holding material). Config block `string_wrap`. Printable paper tape `assets/string_tape.svg` (generated). Complementary to the ring: go/no-go vs continuous.

**Survival (2026-09-19) — read `RECOVERY.md` first if anything is broken or lost**
  independent copies: `C:\APPS\MilkWise` (source), `D:\Dev\MilkWise` (backup clone, never edited,
  refresh with `git -C D:\Dev\MilkWise pull`), the private GitHub repo, and
  `dist\MilkWise_PWA_POC_v0.1.0-poc.zip` (rebuild: `python tools\make_portable.py`; the zip is
- `app/vercel.json` sets no-cache on `/`, `index.html`, `sw.js` and `manifest.webmanifest`, the manifest's content type, `cleanUrls: false`. **Bump `CACHE` in `app/sw.js` on every shipped change** or returning phones keep the old shell. `app/.vercelignore` keeps `.env*`, `.vercel` and the local launchers out of the deployment; `.env.local` holds a Vercel OIDC token and must never be committed or served (verified 404).
- Hosting checks: `python tests\test_hosting.py https://milkwise.vercel.app` (9 checks) and `python tests\test_walkthrough.py https://milkwise.vercel.app` (39 steps) — both take an origin, so they run against the live site, not just localhost.

- **App footage must be a 2× screenshot sequence, never Playwright's video recorder** — with `device_scale_factor=2` `record_video` pads a 1× surface into the requested size, and a CDP screencast is 1× too (measured 2026-09-12). The 860×1860 source is drawn into a 390×844 phone screen: downsample only, never upscale.
- **The camera chapter never uses Chromium's synthetic test pattern.** `tour_fakecam.py` builds a real file for `--use-file-for-fake-video-capture` from the app's own printed calibration sheet, under-lit then brightening, so the light/evenness/stillness gates run for real. `--photo <file>` swaps in a genuine photograph.
- Every shot is reviewed by eye from `contact/` with a PASS/THIN report **before** rendering; audit every spoken number against `app/js/config.js` (that is how "traditional plus three to five millimetres" was caught).

- ⚠️ The assessment (§5.4) proposed **8760–8769** for the API. That block was already held by QI Media Studio (moved there 2026-09-09), so **8770–8779** was allocated instead. Do not use 8760–8769.
- Conventions for folders, file names and docs come from `C:\QIH\ecosystem\QI_Standards.md`; compliance is checked with `python C:\QIH\ecosystem\qi_validator.py --project milkwise`. MilkWise is deliberately NOT a scaffolded QI api/ui project — it is a dependency-free PWA, so the wizard's skeleton does not apply to it.
- QI Brain still has no `milkwise` project, so `qi.log_session` / `qi.log_decision` 400. Registry entry ≠ Brain entry.
**Design rules from the owner (2026-09-11)**
- **No skin-tone artwork.** Never draw phantoms, targets, icons or illustrations with solid fills that could read as a skin colour. Use outlines only, or light yellow / green / pink / blue tints. (Pigmented *silicone* phantoms for infrared testing in Phase 0 are an engineering matter, not artwork — keep them out of consumer-facing material.)
- The soft natural-fibre measuring string and the die-cut ring are the intended aids; printouts are the fallback for people with no other way to measure. Word the app that way.
- Areola dimensions and tissue elasticity are **optional logged context** (`context` block, schema field `context`), never inputs to the sizing rule until outcome data justifies it.

**Do not** add analytics, third-party scripts, image persistence, or any diagnostic claim. Do not rename the product to anything containing "Flange Fit".

## Entry points
`Deploy_MilkWise.bat`
